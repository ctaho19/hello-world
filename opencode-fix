
# Create the fake package structure
mkdir -p ~/.opencode_overrides/node_modules/@ai-sdk/openai-compatible

# Copy your bundle
cp /Users/uno201/opencode/opencode-offline-bundle/github-copilot-provider.bundle.cjs ~/.opencode_overrides/node_modules/@ai-sdk/openai-compatible/index.cjs

# Create package.json
cat > ~/.opencode_overrides/node_modules/@ai-sdk/openai-compatible/package.json << 'EOF'
{
  "name": "@ai-sdk/openai-compatible",
  "version": "9999.0.0-local",
  "main": "index.cjs",
  "type": "commonjs"
}
EOF

# Add to your shell profile for permanent use
echo 'export NODE_PATH="$HOME/.opencode_overrides/node_modules"' >> ~/.zshrc

# Apply now
export NODE_PATH="$HOME/.opencode_overrides/node_modules"

# Test
OPENCODE_DISABLE_DEFAULT_PLUGINS=1 opencode
